#include <stdio.h>

// bubble sort


/* O(mergesort) == O(n log n) 

   The advantage of merge sort is that we continue dividing the unsorted array in halfs until 
   we can start putting the pieces back together. The dividing in half will take log_2(n) ~ log(n).
   
*/

// recursive implementation
int * merge_r(int * unsorted) {
	
}

// iterative impl 






int main() {
	int arr[] = 
}
